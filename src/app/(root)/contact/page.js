import BaseLayout from "@/app/components/BaseLayout";

export const metadata = {
  title: "Contact | Mukesh Sharma",
};

export default function Contact() {
  return (
    <BaseLayout>
    <div>
      <h1>Contact</h1>
    </div>
    </BaseLayout>
  );
}
